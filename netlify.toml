[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Enable SPA fallback if you later add client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200